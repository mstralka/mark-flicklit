# Database Configuration
# For local development with PostgreSQL (requires local PostgreSQL server)
DATABASE_URL="postgresql://postgres:password@localhost:5432/flicklit_dev?schema=public&connection_limit=50&pool_timeout=120&connect_timeout=30"

# JWT Configuration
JWT_SECRET="dev-secret-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Frontend Configuration
VITE_API_URL=http://localhost:3001

# Password Configuration
BCRYPT_ROUNDS=12

# Example production configurations:

# PostgreSQL with SSL
# DATABASE_URL="postgresql://username:password@hostname:5432/flicklit?schema=public&sslmode=require"

# PostgreSQL with connection pooling
# DATABASE_URL="postgresql://username:password@hostname:5432/flicklit?schema=public&connection_limit=10&pool_timeout=60"

# Neon (PostgreSQL-compatible)
# DATABASE_URL="postgresql://username:password@hostname.neon.database.com/flicklit?schema=public&sslmode=require"

# Supabase (PostgreSQL-compatible)
# DATABASE_URL="postgresql://postgres:password@db.project.supabase.co:5432/postgres?schema=public"

# Note: Copy this file to .env and configure for your environment